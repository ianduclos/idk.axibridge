.venv/
__pycache__/
*.pyc
.DS_Store
*.egg-info/
.claude/settings.local.json
graphify-out/
checkpoints/
node_modules/
# built frontend — `npm run build` regenerates it; the server falls back to
# the source in axibridge/static/ when it is absent (see app.py)
axibridge/static_dist/
