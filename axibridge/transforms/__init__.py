"""Transform modules: pure functions mapping one PathDocument to another.

Since v2 these power the plot-pass optimisation step (over resolved
geometry). For per-layer creative reshaping, write an Effect instead — see
docs/MODULES.md.
"""
