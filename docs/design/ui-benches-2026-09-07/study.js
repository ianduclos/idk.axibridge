// Deliberately disconnected design study. All geometry comes from saved fixtures.
const $=s=>document.querySelector(s);
let view='compose', lastBench='second', expanded=false, controls=false, compare=false, failed=false, armed=false, turn=11, alternative=false, kept=false, keptName='Reading 2', step=300, layersWide=false;
const descriptions={compose:['1 · Compose: editing scope stays above the fold.','A 330px inspector gives the paper more room. A compact layer list stays present and expands upward when composition needs it. New material and the selected layer have distinct groups; the Benches group stays in the generator picker.'],second:['2 · Second Reading: the exchange has its own working surface.','The drawing and four turn actions stay together. Response controls apply to the next turn; new-drawing setup is separate. Alternatives can be inspected side by side in the same 280 × 198 mm frame. Closing returns to the composition and pauses this working draft.'],homeostat:['3 · Homeostat: tune a process, inspect what it did.','Parameters sit beside the drawing on desktop; on compact windows they open below it. Step playback and optional telemetry belong to Homeostat. No Your turn, branch history or human-event vocabulary is imposed here. Changing a parameter describes regeneration of the whole run.']};
function field(label,value,opts=''){return `<div class="field"><label>${label}${opts||`<output>${value}</output><input type="range" aria-label="${label}" min="0" max="100" value="${Number(value)*100||50}">`}</label></div>`}
function number(label,value){return `<div class="field"><label>${label}<input type="number" value="${value}" aria-label="${label}"></label></div>`}
function group(title,body,open=false,summary=''){return `<details ${open?'open':''}><summary>${title}<span class="summary-value">${summary}</span></summary>${body}</details>`}
function imageName(){return view==='homeostat'?`homeostat-${step}`:alternative?'second-alternative':`second-${turn}`}
function paper(){return `<div class="paper-wrap" style="--paper-ratio:${view==='homeostat'?300/218:280/198}"><img src="assets/${imageName()}.svg" alt="${view==='homeostat'?'Homeostat, two coupled pens':'Recorded Second Reading exchange'} — example geometry"></div>`}
function layerList(){return `<div class="layers ${layersWide?'expanded':''}"><div class="layers-head"><strong>Layers · ${kept?3:2}</strong><button data-action="layers">${layersWide?'Compact list':'Expand list'}</button></div><button class="layer-row selected"><span class="swatch"></span>Reading 1<small>source</small></button><button class="layer-row"><span class="swatch"></span>Homeostat<small>source</small></button>${kept?`<button class="layer-row"><span class="swatch"></span>${keptName}<small>new</small></button>`:''}${layersWide?'<p class="group-note">Top draws last and can occlude layers below. Expansion gives the stack room without hiding it behind another tab.</p><div class="row"><button disabled>Duplicate</button><button disabled>Consolidate</button></div>':''}</div>`}
function compose(){return `<div class="compose"><section class="canvas-area"><div class="toolbar"><button aria-pressed="true">Select</button><button>Draw</button><button>Pen</button><button class="secondary-tool">Brush</button><button class="secondary-tool">Shape</button><span class="spacer"></span><button data-action="fit">Fit</button></div><div class="stage"><div class="bed-sheet"><img src="assets/compose.svg" alt="Selected Second Reading layer on a portrait composition"></div></div><div class="canvas-foot">Selected: Reading 1 · source frame 280 × 198 mm</div></section><aside class="inspector"><div class="inspector-scroll"><div class="scope"><small>Editing selected layer</small><strong>Reading 1</strong><span class="muted">Second Reading · turn 11</span><p><button class="wide" data-action="open-second">Resume in bench ↗</button></p></div>${group('Placement',`<div class="pair">${number('X · mm','15')}${number('Y · mm','12')}</div><div class="pair">${number('Scale · %','68')}${number('Rotate · °','90')}</div>`,true)}${group('Source & effects','<p class="group-note">Recorded source · edit the recipe in its bench.</p><button disabled>Add effect</button>',false,'0 effects')}${group('New material',`<label class="group-note" for="picker">Generator</label><select id="picker" class="wide"><option value="flow">Flow field</option><optgroup label="Benches"><option value="second">Second Reading</option><option value="homeostat">Homeostat</option><option value="venation">Venation</option><option value="grammar">Grammar</option></optgroup><optgroup label="Image-driven"><option value="image">Image threshold</option></optgroup></select><p class="group-note" id="picker-hint">Choose a generator to create another layer.</p><button id="open-picked" class="wide">Open generator</button>`,true)}${group('Import & assets','<button disabled>Import SVG or image</button>')}${group('Paper guide','<p class="group-note">A4 · portrait view. View rotation does not redefine the machine frame.</p>')}</div>${layerList()}</aside></div>`}
function secondSide(){return `<div class="scope"><small>Working alternative</small><strong>${alternative?'Reading 2':'Reading 1'}</strong><span class="muted">${kept?'Kept as a new layer':'Unkept · memory only'}</span></div>${group('Next turn',`${field('Response mode','',`<select aria-label="Response mode"><option>Shape experiment</option><option>Responsive</option><option>Relation experiment</option><option>First response policy</option></select>`)}${field('Attention','.50')}${field('Departure','.50')}${field('Scale','.50')}<p class="group-note pending" id="pending">Changes here apply on Continue, at turn ${turn+1}.</p>`,true)}${group('Your pen',`${field('Pen smoothing','.60')}<p class="group-note">For the next captured stroke. Earlier strokes keep their recorded smoothing.</p>`)}${group('Alternatives',`<div class="alternatives"><button class="alt ${!alternative?'selected':''}" data-action="alt1"><img src="assets/second-11.svg" alt="Reading 1 thumbnail">Reading 1 · 11</button><button class="alt ${alternative?'selected':''}" data-action="alt2"><img src="assets/second-alternative.svg" alt="Reading 2 thumbnail">Reading 2 · 12</button></div><button class="wide" data-action="compare">${compare?'Return to one drawing':'Compare at shared scale'}</button><p class="group-note">Same frame; no ranking. Reading 2 branches at turn 12.</p>`,true)}${group('New drawing',`<p class="group-note">Separate from the current exchange. These settings apply only to a new drawing.</p>${field('Boundary','',`<select aria-label="Boundary"><option>Overshoot + fit element</option><option>Clip</option><option>Turn inside</option></select>`)}<div class="pair">${number('Width · mm',280)}${number('Height · mm',198)}</div>${number('Seed',12)}<button disabled>New drawing</button>`,false,'280 × 198')}${group('Process details','<p class="group-note">Recorded strokes at turns 5 and 10. Shape experiment; overshoot with whole-element fit. This is a provenance view, not a judgement of the drawing.</p>')}`}
function telemetry(){return `<div class="telemetry"><small>Surprise · illustrative trace, not fixture measurements</small><svg viewBox="0 0 260 75" role="img" aria-label="Illustrative surprise trace with viable band"><rect x="0" y="28" width="260" height="28" fill="var(--flexoki-green-950)"/><path d="M0 58L10 56 20 41 30 35 40 29 50 37 60 57 70 63 80 27 90 13 100 18 110 41 120 44 130 49 140 24 150 17 160 36 170 39 180 52 190 57 200 30 210 23 220 26 230 34 240 39 260 45" fill="none" stroke="var(--flexoki-cyan-400)"/><path d="M75 0V75M147 0V75M206 0V75" stroke="var(--flexoki-yellow-400)" stroke-dasharray="2 4"/></svg><small>Band 0.10–0.50 · marks: rerolls</small></div>`}
function homeSide(){return `<div class="scope"><small>New-layer bench</small><strong>Homeostat</strong><span class="muted">Two coupled pens · seed 42</span></div>${group('Viability',`${field('Measure','',`<select aria-label="Measure"><option>Surprise</option><option>Crowding</option><option>Coverage</option><option>Tangle</option></select>`)}${field('Target','.30')}${field('Tolerance','.20')}${number('Patience',8)}<p class="group-note">Parameter changes regenerate the run from its seed. They do not intervene at this step.</p>`,true)}${group('Hand & coupling',`${number('Pens',2)}${field('Variety','.35')}${field('Escalation','.40')}`,false,'2 pens')}${group('Telemetry',telemetry(),true)}${group('Frame & seed',`<div class="pair">${number('Width · mm',200)}${number('Height · mm',160)}</div>${number('Seed',42)}<p class="group-note">A seed and exact parameters identify a repeatable run.</p>`)}${group('Advanced','<p class="group-note">Step length, occupancy grid and memory controls remain reachable here.</p>')}`}
function bench(){let second=view==='second';return `<section class="bench ${controls?'show-controls':''}" aria-label="${second?'Second Reading':'Homeostat'} bench"><header class="bench-head"><div><h2>${second?'Second Reading':'Homeostat'} <span class="muted">/ bench</span></h2><p>${second?'From Reading 1 · working independently':'New material · composition unchanged'}</p></div><span class="spacer"></span><button class="compact-controls" data-action="controls" aria-expanded="${controls}">${controls?'Hide controls':'Controls'}</button>${$('#presentation').value==='popup'?`<button data-action="expand">${expanded?'Restore':'Expand'}</button>`:''}<button data-action="return">← Compose</button></header><div class="bench-body"><div class="bench-main"><div class="bench-state"><strong>${second?(alternative?'Reading 2':'Reading 1'):'Run 1 · bed 300 × 218 mm'}</strong><span>${second?`Turn ${alternative?12:turn}`:`Step ${step}`}</span><span class="spacer"></span><span>${failed?'Last preview · stale':'Preview current'}</span></div><div class="stage ${armed?'armed':''}">${compare&&second?`<div class="compare"><figure><img src="assets/second-11.svg" alt="Reading 1, turn 11"><figcaption>Reading 1 · turn 11</figcaption></figure><figure><img src="assets/second-alternative.svg" alt="Reading 2, turn 12"><figcaption>Reading 2 · turn 12</figcaption></figure></div>`:paper()}${armed?'<span class="capture-label">Your turn · click to rehearse the recorded stroke</span>':''}</div>${failed?'<div class="failure" role="alert">Preview unavailable. Previous drawing shown.<button data-action="retry">Retry this preview</button></div>':''}<div class="transport">${second?'<button data-action="undo">Undo in bench</button>':'<button data-action="play">Play steps</button>'}<label for="scrub">${second?'Turn':'Step'}</label><input id="scrub" type="range" aria-label="${second?'Turn':'Step'}" min="${second?11:300}" max="${second?13:600}" step="${second?1:150}" value="${second?turn:step}"><span>${second?turn:step}</span><button data-action="fit">Fit</button></div><div class="action-strip">${second?`<button data-action="your-turn" aria-pressed="${armed}">${armed?'Cancel stroke':'Your turn'}</button><button data-action="continue">Continue one turn</button><button data-action="another">Try another</button><button class="primary" data-action="keep" ${failed?'disabled':''}>Keep as layer</button>`:`<span class="muted">${step} steps · seed 42</span><button class="primary" data-action="keep" ${failed?'disabled':''}>Create layer at step ${step}</button>`}</div></div><aside class="bench-side">${second?secondSide():homeSide()}</aside></div></section>`}
function render(){document.querySelectorAll('[data-view]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.view===view)));$('#caption').textContent=view==='compose'?'C1 · Persistent layers + explicit selection. Expand Placement or New material to inspect the hierarchy.':`${$('#presentation').value==='embedded'?'A · Workspace presentation':'B · Popup presentation'} · Same bench identity and working state. Compact windows prioritise the drawing; Controls reveals the secondary shelf.`;$('#note-title').textContent=descriptions[view][0];$('#note-body').textContent=descriptions[view][1];$('#draft-chip').hidden=view!=='compose'||lastBench===null;$('#target').textContent=view==='compose'?'Plot target: composition':'Bench draft · return to Compose to plot';$('#fail').disabled=view==='compose';$('#work').innerHTML=view==='compose'?compose():($('#presentation').value==='popup'?`<div class="overlay-background"><img src="assets/second-11.svg" alt="Dimmed composition context"></div><div class="popup-wrap ${expanded?'expanded':''}">${bench()}</div>`:bench());wire();}
function notice(text){$('#notice').textContent=text}
function setView(v){if(v!=='compose')lastBench=v;view=v;failed=false;armed=false;compare=false;render()}
function wire(){document.querySelectorAll('[data-action]').forEach(b=>b.onclick=()=>action(b.dataset.action));const picker=$('#picker');if(picker){picker.onchange=()=>{$('#open-picked').textContent=['second','homeostat','venation','grammar'].includes(picker.value)?'Open bench ↗':'Open generator';$('#picker-hint').textContent=['second','homeostat','venation','grammar'].includes(picker.value)?'A dedicated bench; new material stays separate from the selected layer.':'Choose a generator to create another layer.'};$('#open-picked').onclick=()=>{if(['second','homeostat'].includes(picker.value))setView(picker.value);else notice('This study covers Compose, Second Reading and Homeostat. Other picker entries show proposed discovery only.')}}const scrub=$('#scrub');if(scrub)scrub.oninput=()=>{if(view==='second'){turn=Number(scrub.value);alternative=false}else step=Number(scrub.value);render()};document.querySelectorAll('.field input[type=range]').forEach(input=>input.oninput=()=>{input.previousElementSibling.textContent=(Number(input.value)/100).toFixed(2);if($('#pending'))$('#pending').textContent=`Queued for turn ${turn+1} · display-only in this study`;notice('Parameter affordance previewed. The stored example geometry is unchanged.');});if(armed)$('.stage').onclick=()=>{armed=false;turn=12;alternative=false;render();notice('Recorded example loaded to rehearse the end of a human turn. No stroke was captured.')}}
function action(a){switch(a){case'open-second':setView('second');break;case'layers':layersWide=!layersWide;render();break;case'return':view='compose';armed=false;render();notice('Returned to Compose. Bench draft paused; turn and alternative remain in this page’s memory.');break;case'controls':controls=!controls;render();break;case'expand':expanded=!expanded;render();break;case'your-turn':armed=!armed;render();break;case'continue':armed=false;turn=Math.min(13,turn+1);alternative=false;render();notice('Next recorded example shown. This study contains turns 11–13.');break;case'another':alternative=true;turn=12;controls=true;render();notice('Reading 2 shown: a recorded branch at turn 12. Reading 1 remains available.');break;case'alt1':alternative=false;turn=11;render();break;case'alt2':alternative=true;turn=12;render();break;case'compare':compare=!compare;render();break;case'undo':turn=Math.max(11,turn-1);alternative=false;render();notice('Rehearsed bench-local undo. Composition unchanged.');break;case'keep':kept=true;keptName=view==='second'?'Reading 2':'Homeostat 2';render();notice(view==='second'?'Keep rehearsed: a new layer would receive this exact recipe. Working alternatives remain. Save project is still a separate action.':'Create rehearsed: a new layer would use this seed, parameters and visible step. The original layer is not changed.');break;case'play':step=step===600?300:step+150;render();notice('Next stored step shown. Playback is sampled in this mockup; no process is running.');break;case'retry':failed=false;render();notice('Recovery state rehearsed. The same preview is current again; no turn advanced.');break;case'fit':notice('Fit shows the complete source frame. Thumbnails and comparison use the labelled frame, not ink bounds.');break;}}
document.querySelectorAll('[data-view]').forEach(b=>b.onclick=()=>setView(b.dataset.view));$('#size').onchange=()=>{$('#window').className=$('#size').value;};$('#presentation').onchange=render;$('#resume').onclick=()=>setView(lastBench);$('#fail').onclick=()=>{failed=!failed;render()};document.addEventListener('keydown',e=>{if(e.key==='Escape'&&view!=='compose'){if(armed){armed=false;render()}else action('return')}});lastBench=null;render();
